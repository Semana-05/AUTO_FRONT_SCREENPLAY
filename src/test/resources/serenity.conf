serenity {
  take.screenshots = FOR_FAILURES
  restart.browser.for.each = feature
}

headless.mode = false

webdriver {
  driver = chrome
  timeouts {
    implicitly.wait = 5000
    wait.for.element = 15000
    page.load = 30000
  }
  capabilities {
    browserName = "chrome"
    acceptInsecureCerts = true
    "goog:chromeOptions" {
      args = ["remote-allow-origins=*", "test-type", "no-sandbox", "ignore-certificate-errors",
              "--window-size=1000,800", "disable-infobars", "disable-gpu",
              "disable-default-apps", "disable-popup-blocking", "disable-dev-shm-usage",
              "disable-extensions", "disable-web-security", "disable-translate", "disable-logging"]
    }
  }
}

environments {
  default {
    webdriver.base.url = "http://localhost:80/dashboard"
  }
}